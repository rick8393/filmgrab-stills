---
name: filmgrab-stills
description: "通过豆包AI浏览器运行 FILMGRAB 影片剧照下载 RPA。"
---

# FILMGRAB 影片剧照下载

## 使用环境确认（最先执行）

- 在收集任务信息、连接浏览器或创建浏览器工作区前，先从当前智能体宿主提供的执行环境标识、System Context 或运行能力元数据中判断当前是否处于豆包本地电脑模式。不要向用户询问本地电脑或云电脑。
- 环境标识明确为云电脑时，立即终止本次流程，只回复："该功能仅支持本地电脑，请切换豆包的本地电脑模式后再试。"不要收集任务参数，不要连接浏览器，也不要提供替代执行方式。
- 环境标识明确为本地电脑时，先运行 `local setup status`。豆包客户端最低支持版本为 2.28.8。若 `app_version_gate_code=DOUBAO_VERSION_UNSUPPORTED`，只能原样回复："当前豆包版本过低，请升级豆包至最新版后重试。"然后立即停止。不得向用户提问、给出选项或其他继续方式，也不得提及 dev/测试环境、版本覆盖或内部诊断。
- 如果版本检测失败（`app_version_detected=false`），仅保留诊断信息，继续环境初始化和当前任务，不询问用户确认，不阻塞回放；只有明确检测到低版本才停止。
- `local setup status` 是只读检查。浏览器未启动、无窗口或暂未连接时，仍执行下文的 `node scripts/run.js`，由运行器在业务开始前主动拉起豆包浏览器并检查可用性；不要只等待或重建开发会话。只有运行器自动恢复失败后，才提示："豆包豆包浏览器自动化助手暂时不可用。请将豆包升级至最新版，或完全退出后重新打开豆包，再重试。"不要引导普通用户安装、重载或修复插件。
- 环境标识缺失、冲突或无法可靠判断时，不要猜测、不要询问用户，也按不支持处理，只回复上述本地电脑模式提示。

## 必填信息

- 本任务没有需要用户额外填写的必填信息。

## 与用户沟通

- 先说明当前进展，再说明用户是否需要操作，最后只给出一个下一步。
- 只使用“豆包浏览器自动化助手”“可用”“使用中”“允许使用豆包内置浏览器”“结束当前任务”等产品词汇。
- 对用户说“任务说明”“自动完成步骤”“结果核对”，不要说 PRD、命令、JSON、错误码、文件路径或内部诊断信息。
- 正在自动恢复时，说“正在恢复豆包浏览器自动化助手，请稍候”。只有登录、验证码、授权或确认需要用户亲自完成时才提问。
- 完成后直接返回业务结果，并说明结果已经核对；未完成时说明用户能理解的原因和一个下一步。

## 结果展示

- 成功结果包含多条记录时，必须使用 Markdown 表格展示真实结果：一条记录一行，已确认的业务输出字段作为列。不得用项目符号、编号列表、散文、原始 JSON 或“每条记录包含”之类的字段定义代替结果表格。
- 成功结果只有一条结构化记录且包含两个及以上业务字段时，必须使用“字段 | 结果”两列表格。URL 保留完整值并在支持时显示为可点击链接。
- 不要把 `success`、`validated`、trace、内部 ID 等校验字段展示成业务列。只有结果确实为空时才可以不输出表格，并明确说明没有找到结果及用户可理解的原因。

## 执行

收集齐必填信息后，将本次用户输入写入 UTF-8 JSON 文件并运行：

```text
node scripts/run.js --param-file /absolute/path/to/params.json
```

不要把 JSON 放进 shell 命令。不要向用户展示录制示例、开发验收数据、内部参数或调试默认值。解析 stdout 的单一 JSON，要求 `success=true`，返回业务结果。
每次调用都只使用豆包AI浏览器中的豆包浏览器自动化助手。运行器会在业务执行前自动恢复当前客户端的浏览器环境；收到恢复进度时等待本次调用结束，不要重复启动业务任务。只有用户此前主动停止过允许使用时，才请用户打开助手并点击“允许使用豆包内置浏览器”。不得因为浏览器曾关闭而直接要求升级或重启；只在运行器恢复失败后给出对应提示。不要索取连接码，不要切换到其他浏览器环境。
